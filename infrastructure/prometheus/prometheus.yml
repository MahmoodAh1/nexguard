# Prometheus scrape configuration for NexGuard.
global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: nexguard-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: nexguard-api
